[package]
  cjc-version = "1.0.5"
  name = "glyphnet_bench"
  description = "GlyphNet performance benchmark vs paramiko/netmiko/Go."
  version = "0.1.0"
  output-type = "executable"

[dependencies]
  glyphnet = { path = "../.." }

[ffi.c]
  ssh2 = { path = "../../vendor/lib" }
